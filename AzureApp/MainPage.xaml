<Page
    x:Class="AzureApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel x:Name="contentPanel" Margin="8,32,0,0">
            <TextBlock Text="UserName:"/>
            <StackPanel x:Name="inputPanel" Orientation="Horizontal" Margin="0,20,0,20">
                <TextBox x:Name="userName" Width="280" HorizontalAlignment="Left"/>
            </StackPanel>
            <Button Content="Execute" Height="31" Width="92" Margin="5,9,0,0" Click="Button_Click"/>
            <ListBox x:Name="detailList" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="400"/>
        </StackPanel>
    </Grid>
</Page>
